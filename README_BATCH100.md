# TORC Batch 100

Two surgical corrections only.

1. Navigation: `app/nav.module.css` is restored wholesale to the last known-good pre-Batch-97 navigation stylesheet. This removes the Batch 97/98/99 sizing and breakpoint overrides instead of trying to override them yet again. TORC lockup and navigation therefore return to the established site navigation behavior.
2. Artifacts upper oxblood guides: approved line weight retained; upper endpoint moved exactly 1px upward from Batch 99 (`top:-55px; height:55px`).

Apply after Batch 99.
