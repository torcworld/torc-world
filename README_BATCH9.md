# TORC Batch 9 — Compare tone + warm grey surface trial

Apply after Batch 8.

Changes:
- Compare result band changed from near-black `#171714` to dark charcoal `#292927`.
- Order/CMS separator changed to TORC oxblood `#5b2630`.
- Site-wide background/surface trial: `--bg` is now warm grey `#e9e8e4`; `--paper` is now `#f1f0ec` to remove stark white surfaces while preserving contrast and the existing TORC palette.
- No Compare empty-state redesign yet; intentionally deferred until the new tonal hierarchy is judged in context.
