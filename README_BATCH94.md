# TORC Batch 94 — exact site ground + finer full-height hero-edge guides

Apply after Batch 93.

Changed only `app/artifacts/artifacts-v4.module.css`.

Desktop only:
- replaces the too-dark Batch 93 ground with the actual global TORC background `#f5f2ea` used by the site/Submit/Theory family;
- keeps the two oxblood guides exactly on the left/right edges of the Artifacts hero;
- softens their opacity so the 1 CSS-pixel rule reads finer;
- extends them 52px upward through the pre-hero gap to meet the upper navigation guide.

No mobile, data, routing, corpus, analysis, or Theory changes.
