# TORC Artifacts — darker gold ONLY

Changed only:
- `app/artifacts/artifacts-overhaul.module.css`

Correction:
- Only the existing yellow/gold color `#c7aa76` is changed to the darker gold `#b7955d`.
- Matching rgba uses of that same gold are changed accordingly.
- The lighter brown / ochre / sand colors are preserved exactly as they were, including `#d9c7a6`.
- No layout, spacing, behavior, borders, charcoal, burgundy, or other colors are changed.
- Based on the pre-color-test pyramid-rule Artifacts state, so the previous over-broad gold replacement is not carried forward.
