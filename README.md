# TORC Organizations active-nav patch

Changed only:
- `app/TorcNav.tsx`

Change:
- Organizations now receives the existing `orgActive` state while the visitor is on `/for-organizations`.
- This restores the persistent gold Organizations navigation button with its red/burgundy lettering.
- No other navigation label, route, styling, or page content is changed.
