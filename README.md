# TORC Artifacts — hero rule matches visible search/filter fields

Changed only:
- `app/artifacts/artifacts-overhaul.module.css`

Fix:
- The previous attempt aligned the black rule to the outer tools container.
- This patch aligns it to the actual visible row of four fields inside that container.
- Relative to the previous 4mm-shorter variant, the line is shortened by about 2.9px per side.
- Vertical position is unchanged.
- Nothing else is changed.
