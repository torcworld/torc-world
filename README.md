# TORC Artifacts — exact search-column rule width

Changed only:
- `app/artifacts/artifacts-overhaul.module.css`

Change:
- The black line beneath the charcoal hero is now aligned exactly with the visible outer edges of the full four-field Search / Sort / Medium / Operational Order row.
- This accounts for the 24px inner padding of the tools container.
- Vertical position is unchanged.
- Nothing else is changed.
