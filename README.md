# TORC Artifacts — top frame matches bottom

Changed only:
- `app/artifacts/artifacts-overhaul.module.css`

Desktop change only:
- The top charcoal Artifacts hero is inset slightly farther from both gold side rails, matching the Compare block's floating treatment.
- Adds a small amount of light-brown breathing room above and below the charcoal hero.
- Removes the old attached gold band directly underneath the hero.
- Adds one thin black/charcoal rule beneath the hero after that small gap.
- Does not alter the hero's content, height, typography, search controls, archive, pagination, Compare position, or any previous interaction behavior.
