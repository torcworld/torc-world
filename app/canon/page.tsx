import Link from 'next/link'; import {artifacts} from '@/lib/artifacts';
const order=['M','GΩ','G','3C','3B','3A','2','1'];
export default function Canon(){const rows=[...artifacts].sort((a,b)=>{const ao=order.indexOf(a.torc.toUpperCase()),bo=order.indexOf(b.torc.toUpperCase());return ao===bo?b.cms-a.cms:ao-bo});return <main><header className="page-title"><div className="eyebrow">Public corpus</div><h1>The Canon</h1><p className="lede">Artifacts ranked lexicographically by Operational Order, then Cognitive Magnitude.</p></header><div className="table"><div className="row header"><span>Artifact</span><span>Domain</span><span>TORC</span><span>CMS</span></div>{rows.map(a=><Link className="row" key={a.slug} href={`/artifact/${a.slug}`}><span><b>{a.title}</b><br/><span className="small">{a.creator} · {a.year}</span></span><span>{a.domain}</span><span className="score">[{a.torc}]</span><span className="score">[{a.cms}]</span></Link>)}</div></main>}
