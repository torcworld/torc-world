import Link from 'next/link';import {magicMountain as m} from '@/lib/magicMountain';export default function Report(){return <main><header className="page-title"><div className="eyebrow">Consensus report · TORC/DIP</div><h1>The Magic Mountain</h1><p className="lede">Seven-evaluator aggregation record</p></header><section className="section prose"><h2>Result</h2><div className="bigDip">3b <i>|</i> 840</div><p><b>TORC distribution:</b> {m.torcDistribution}<br/><b>CMS median:</b> 840 · <b>range:</b> {m.cmsRange}</p><h2>Operational consensus</h2><p>All seven evaluators place the artifact within Level 3. The contested boundary is 3b→3c: four judge the recursive architecture insufficiently stabilized into a globally governing integration, while three regard the integration as sufficient for 3c. All seven reject G.</p><h2>Dimensional synthesis</h2>{m.dimensions.map(d=><div className="reportDim" key={d.key}><h3>{d.key} — {d.name}: {d.score}</h3><p>{d.rationale}</p><p className="small">Evaluator range: {d.range}</p></div>)}<h2>Evaluator record</h2>{m.evaluators.map(e=><p key={String(e[0])}><b>{e[0]}</b> — {e[1]} | {e[2]}</p>)}<p className="small">Complete untouched individual evaluator reports can be attached here as the audit archive is populated.</p><Link className="button" href="/artifact/the-magic-mountain">← Back to artifact</Link></section></main>}