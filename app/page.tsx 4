import Link from 'next/link'; import {artifacts} from '@/lib/artifacts';
const featured=['pale-fire','on-formally-undecidable-propositions-of-principia-mathematica-and-related-systems-i','2001-a-space-odyssey','the-art-of-fugue','kasparov-vs-topalov-wijk-aan-zee-1999','fountain'];
export default function Home(){const picks=featured.map(s=>artifacts.find(a=>a.slug===s)).filter(Boolean);return <main><section className="hero"><div className="eyebrow">Demonstrated intelligence in artifacts</div><h1>Intelligence leaves evidence.</h1><p className="lede">TORC evaluates what cognitive operation an artifact actually performs — and how much structured cognition it demonstrates in performing it.</p><div className="buttons"><Link className="button primary" href="/artifacts">Explore artifacts</Link><Link className="button" href="/theory">How TORC works</Link></div></section><section className="section"><div className="section-head"><div><div className="eyebrow">Artifact database</div><h2>Across domains</h2></div><Link href="/artifacts" className="small">View artifacts →</Link></div><div className="grid">{picks.map((a:any)=><Link className="card" key={a.slug} href={`/artifact/${a.slug}`}><div className="meta">{a.domain} · {a.year}</div><h3>{a.title}</h3><div className="small">{a.creator}</div><div className="dip">{a.status==='evaluated'?`[${a.torc}] | [${a.cms}]`:'Evaluation in progress'}</div></Link>)}</div></section><section className="section commissionBand"><div><div className="eyebrow">Expand the archive</div><h2>Can’t find an artifact?</h2><p className="lede">Commission an independent TORC/DIP evaluation and add it permanently to the archive, with optional commissioner credit.</p></div><div className="buttons"><Link className="button" href="/commission">Commission an artifact →</Link><Link className="button" href="/evaluate-your-work">Evaluate your own work</Link></div></section><section id="organizations" className="section"><div className="eyebrow">TORC/DIP for organizations</div><h2>Private evaluation, comparison, and licensing.</h2><p className="lede">For producers, studios, publishers, research groups, universities, AI teams, and other organizations evaluating complex creative or intellectual work.</p><div className="orgCards"><article><h3>Private evaluation</h3><p>Apply TORC/DIP to unreleased screenplays, manuscripts, narrative projects, research outputs, games, or other original work without adding it to the public archive.</p></article><article><h3>Comparative analysis</h3><p>Compare several artifacts under the same framework—for example, a slate of scripts, submissions, concepts, or competing project options.</p></article><article><h3>Portfolio & batch analysis</h3><p>Map recurring strengths, bottlenecks, and cognitive architectures across a body of work rather than evaluating one artifact in isolation.</p></article><article><h3>Licensing & custom applications</h3><p>Discuss internal use of the TORC/DIP methodology, custom evaluation workflows, or other organizational applications of the framework.</p></article></div><div className="orgInquiry"><b>Clients choose the material and scope—not the result.</b><p>Professional engagements are scoped individually. Pricing, turnaround, confidentiality, and deliverables are agreed before any work begins.</p><div className="buttons"><a className="button primary" href="mailto:contact@torc.world?subject=TORC%2FDIP%20Professional%20Inquiry">Discuss a project</a></div></div></section><section className="section"><div className="eyebrow">The governing distinction</div><p className="manifesto">Prestige is not an evaluator. Difficulty is not an evaluator. Complexity is not an evaluator. TORC asks what operation has actually been demonstrated.</p></section></main>}