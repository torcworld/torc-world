import Link from 'next/link'; import {artifacts} from '@/lib/artifacts';
const featured=['pale-fire','on-formally-undecidable-propositions-of-principia-mathematica-and-related-systems-i','2001-a-space-odyssey','the-art-of-fugue','kasparov-vs-topalov-wijk-aan-zee-1999','fountain'];
export default function Home(){const picks=featured.map(s=>artifacts.find(a=>a.slug===s)).filter(Boolean);return <main><section className="hero"><div className="eyebrow">Demonstrated intelligence in artifacts</div><h1>Intelligence leaves evidence.</h1><p className="lede">TORC evaluates what cognitive operation an artifact actually performs — and how much structured cognition it demonstrates in performing it.</p><div className="buttons"><Link className="button primary" href="/canon">Explore 75 artifacts</Link><Link className="button" href="/theory">How TORC works</Link></div></section><section className="section"><div className="section-head"><div><div className="eyebrow">Launch corpus</div><h2>Across domains</h2></div><Link href="/canon" className="small">View all 75 →</Link></div><div className="grid">{picks.map((a:any)=><Link className="card" key={a.slug} href={`/artifact/${a.slug}`}><div className="meta">{a.domain} · {a.year}</div><h3>{a.title}</h3><div className="small">{a.creator}</div><div className="dip">{a.status==='evaluated'?`[${a.torc}] | [${a.cms}]`:'Evaluation in progress'}</div></Link>)}</div></section><section className="section"><div className="eyebrow">The governing distinction</div><p className="manifesto">Prestige is not an evaluator. Difficulty is not an evaluator. Complexity is not an evaluator. TORC asks what operation has actually been demonstrated.</p></section></main>}