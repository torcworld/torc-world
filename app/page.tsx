import Link from 'next/link'; import {artifacts} from '@/lib/artifacts';
export default function Home(){return <main><section className="hero"><div className="eyebrow">Demonstrated intelligence in artifacts</div><h1>What happened here?</h1><p className="lede">TORC evaluates what cognitive operation an artifact actually performs — and how much structured cognition it demonstrates in performing it.</p><div className="buttons"><Link className="button primary" href="/canon">Explore the Canon</Link><Link className="button" href="/theory">Read the Theory</Link></div></section><section className="section"><div className="section-head"><div><div className="eyebrow">Selected artifacts</div><h2>The Canon</h2></div><Link href="/canon" className="small">View all →</Link></div><div className="grid">{artifacts.map(a=><Link className="card" key={a.slug} href={`/artifact/${a.slug}`}><div className="meta">{a.domain} · {a.year}</div><h3>{a.title}</h3><div className="small">{a.creator}</div><div className="dip">[{a.torc}] | [{a.cms}]</div></Link>)}</div></section><section className="section"><div className="eyebrow">The governing distinction</div><p className="manifesto">Prestige is not an evaluator. Difficulty is not an evaluator. Complexity is not an evaluator. TORC asks what operation has actually been demonstrated.</p></section></main>}
