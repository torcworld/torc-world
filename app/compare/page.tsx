import {artifacts} from '@/lib/artifacts';
export default function Compare(){return <main><header className="page-title"><div className="eyebrow">Side by side</div><h1>Compare</h1><p className="lede">Version one shows the comparison structure. Interactive artifact selection comes next.</p></header><div className="grid">{artifacts.slice(1,3).map(a=><div className="card" key={a.slug}><div className="meta">{a.domain}</div><h3>{a.title}</h3><div className="dip">[{a.torc}] | [{a.cms}]</div><p className="small">RL {a.rl} · AD {a.ad} · CD {a.cd} · ID {a.id} · IC {a.ic} · GR {a.gr}</p></div>)}</div></main>}
