# TORC Batch 58 — Result Registration + Quieter Search Surround

Apply after Batch 57.

Artifacts page only:
- Operational Order values (G, GΩ, 3c, 2, etc.) move further left on desktop so they sit more directly beneath the Operational Order heading.
- The Cognitive Magnitude Score heading moves about half a centimetre to the right.
- CMS values retain their current position rather than following the heading shift.
- The search/filter surround now uses the same background as the Artifacts page itself.
- The thin oxblood border around that search/filter area stays in place.
- The whole search/filter console moves upward by roughly half a centimetre on desktop.
- `Curated TORC analyses` is deliberately left unchanged in this pass so it can be judged separately.

No artifact data, filters, sorting, pagination, Compare behavior, routes, scores, or analyses are changed.
