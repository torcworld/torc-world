# TORC Batch 78 — Left Artifacts Hero + Editorial Corpus Mark + Black Controls

Apply after Batch 77.

Artifacts hero:
- Restores `Artifacts` to warm white.
- Moves the title and `Curated TORC analyses` to the left.
- Adds a restrained editorial counterweight on the right:
  `THE TORC CORPUS` — thin oxblood rule — `DIAGNOSED ARTIFACTS`.
- This is intentionally typographic/architectural rather than a decorative flourish.
- The right-side mark is hidden on mobile.

Search/filter:
- Outer console border remains thin black.
- Small input/select outlines are changed from oxblood to black.
- Focus outlines are black as well.
- Selected values remain black.

Corpus:
- `Shuffle Again` returns to black, including its border and hover/focus state.

No artifact data, scores, filters, sorting logic, routes, pagination, corpus rows, or analysis content are changed.
