# TORC Batch 83 — Home + Methodology House-Style Pass

Apply after the current deployed patch chain.

Files changed:
- `app/page.tsx`
- `app/home.module.css`
- `app/methodology/page.tsx`
- `app/methodology/methodology.module.css`

What this pass does:
- Gives the TORC landing page the same institutional/architectural family as Submit and Organizations: charcoal arrival field, oxblood frame/cap, warm gold surfaces, black perimeter logic, restrained editorial hierarchy.
- Reframes the two-coordinate section so Operational Order is explicitly the primary hierarchy; CMS is separate magnitude and does not flatten Order.
- Restyles the Methodology / seven Operational Orders page from a sparse utility page into a composed TORC system page.
- Makes all seven Orders feel like one explicit hierarchy; G and GΩ receive a distinct oxblood treatment rather than looking like equivalent neutral rows.
- Restyles the six dimensions and proprietary-procedure ending in the same house style.
- No artifact corpus data, analysis pages, navigation logic, pricing, or Organizations/Submit files are touched.

Validation:
- Targeted TypeScript parse produced no syntax diagnostics. Missing Next/CSS-module imports remain expected because dependencies are not installed in this extracted source tree.
