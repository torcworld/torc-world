# TORC Batch 74 — Mobile TOC Open State + Artifact Table Symmetry

Apply after Batch 73.

Theory book — phone:
- Keeps the existing Contents button.
- Makes the opened state explicit on touch devices: when the existing BookNavigator state is open, the Table of Contents panel is forced visible, sized, padded, bordered and scrollable.
- When closed, only the Contents trigger remains.

Artifacts — desktop:
- Adds a black rule below `Artifact / Medium / Operational Order / Cognitive Magnitude Score` matching the black rule above it.
- Gives the header row equal vertical breathing room above and below the labels.
- Changes selected values inside Sort, Medium and Operational Order controls to oxblood.
- Changes the small toolbar action button beneath the filters to oxblood as well.

No artifact data, sorting logic, book text, routes, scores, pagination, navigation logic or analysis content are changed.
