# TORC Batch 101 — clean navigation reset + guide 1px up

Apply after Batch 100.

This patch stops layering navigation fixes. `app/nav.module.css` is replaced by a clean, compact stylesheet matching the established TORC navigation family: visible TORC lockup/subtitle on desktop, full-size light-gold Artifacts/Theory/Submit buttons, oxblood Organizations, deeper-gold active state, and the existing 2x2 mobile family below 900px.

Artifacts: the approved oxblood guide thickness is unchanged; only the upper endpoint moves exactly 1px upward from Batch 100.
