TORC Batch 19 — FIXED

Apply after Batch 18. This replaces the original Batch 19 patch.

Fix:
- Corrects the TypeScript state typing in SubmissionForm so the controlled Analysis select accepts the browser string value without a compile-time type error.
- Retains the Batch 19 Submit Your Artifact redesign and package-to-form selection behavior.
