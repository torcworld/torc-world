# TORC Batch 66 — Closed Artifacts Frame

Apply after Batch 65.

Artifacts page only:
- Keeps one translucent charcoal horizontal rule beneath the global navigation.
- Removes the second horizontal rule above the charcoal Artifacts hero.
- Extends the thin oxblood vertical side rules all the way up to the remaining translucent top rule, forming one clear rectangular page frame.
- Keeps the frame aligned to the existing Artifacts page width: TORC-side on the left, Organizations-side on the right.
- Moves the charcoal Artifacts hero upward so it begins roughly 1.5–2 cm below the top rule.
- The charcoal Artifacts hero now reaches all the way to both vertical oxblood frame lines.
- Search/filter and corpus surfaces remain slightly inset inside the frame.

No navigation logic, artifact data, filtering, sorting, pagination, Compare behavior, analysis pages, scores, or routes are changed.
