# TORC Batch 95 — Submit Ground + Explicit Hero-Corner Guides

Deploy on top of Batch 94.

Only `app/artifacts/artifacts-v4.module.css` changes.

Desktop changes:
- Uses Submit's actual local page-ground token: `#e8e0d4`.
- Replaces the previously inferred upper guide extension with explicit 52px hairlines attached directly to the two top corners of the charcoal Artifacts hero.
- Opens the Batch 91 clip-path at the top so those extensions cannot be clipped away.
- Downward guides start at the same hero-corner axis and continue to the bottom.
- Guides are visually finer via 1px rules scaled to 55% width with softer oxblood opacity.

No mobile changes. No page.tsx, data, routing, analyses, Theory or Submit files are touched.
