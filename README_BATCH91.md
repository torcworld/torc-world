# TORC Batch 91 — Detached Artifacts Composition Trial

Apply after Batch 90.

Desktop Artifacts only:
- removes the continuous side rails/frame;
- leaves the charcoal Artifacts hero as a standalone rectangular block;
- inserts pale site-ground breathing space below the hero;
- makes Search/filters a detached light-ochre panel with a thin oxblood perimeter;
- inserts another pale site-ground gap;
- makes the corpus/list + Compare a second detached light-ochre panel with a thin oxblood perimeter;
- returns to pale site ground below the corpus.

Mobile rules are intentionally untouched.
No TSX, corpus data, analyses, routing, or Theory files are included.
