# TORC Batch 89 — Artifacts pale surround + light controls

Apply after Batch 88.

Changed only `app/artifacts/artifacts-v4.module.css`.

Desktop changes:
- Replaces the charcoal page surround / broad horizontal gaps with the pale neutral `#e6dccd`.
- Keeps the black Artifacts hero.
- Keeps the light-ochre search/corpus surfaces.
- Makes Search, Medium, Operational Order, and Sort controls pale light grey (`#f1ede7`).
- Keeps the black side bands and black lower closure beneath Compare Artifacts.
- Mobile rules are untouched.
