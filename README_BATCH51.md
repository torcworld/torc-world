# TORC Batch 51 — Artifacts Architectural Composition Trial

Apply after Batch 50.

Artifacts-page-only visual pass:
- Adds two substantial oxblood vertical architectural rails on desktop, inset from the browser edge.
- Replaces the old promotional header CTA with a restrained corpus marker:
  `{artifacts.length} artifacts` / `Ranked by demonstrated cognition.`
  The count is dynamic rather than hard-coded.
- Keeps the large Artifacts title as the primary entrance.
- Changes Search / Sort / Medium / Operational Order from stark white controls into a warm broken-grey instrument panel.
- Focus states use oxblood rather than generic black.
- Brings the inline Compare controls into the same quiet warm-grey material family.
- Mobile removes the side rails and stacks the corpus marker naturally.

No corpus entries, rankings, filters, sorting behavior, Compare behavior, routes, analysis content, or scoring data are changed.
