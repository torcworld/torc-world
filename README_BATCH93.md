# TORC Batch 93 — Artifacts exact hero-edge frame + warm ground

Apply after Batch 92.

Desktop-only correction:
- moves the two oxblood vertical frame rules from 28px outside the composition to the exact left/right edges of the wide charcoal Artifacts hero;
- extends those rules upward through the pale gap to meet the upper page/navigation guide;
- removes the little horizontal shoulders introduced in Batch 92;
- changes the broad side fields and all exposed gaps between detached blocks from the cooler pale `#e6dccd` to the warmer TORC desktop ground `#ddceb3`, matching the navigation/Submit-family surround;
- leaves the detached charcoal hero, ochre search panel, ochre corpus panel, table, data, and mobile layout unchanged.

Only `app/artifacts/artifacts-v4.module.css` is included.
