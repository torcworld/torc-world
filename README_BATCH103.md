# TORC Batch 103 — uncover navigation + rewrite two public analyses

Apply after Batch 102.

Navigation / Artifacts
- Removes the Artifacts page's `100vmax` full-bleed box-shadow and inherited clip-path on desktop.
- The body already uses the exact same `--bg`, so the approved background colour is preserved.
- This prevents the later-rendered Artifacts page paint from extending upward over the root navigation.
- Keeps the approved oxblood guide geometry/weight from Batch 101 unchanged.

Public analyses
- Rewrites *A Confederacy of Dunces* and *Genius: The Natural History of Creativity* in the artifact-specific editorial style used by the strongest corpus pages.
- Removes all references to evaluator counts, run ranges, medians, protocol procedure, and other internal calibration language from the public prose.
- Keeps the stabilized corpus values: 2 | 743 and G | 833, with their median dimensional profiles.
- Generic public artifact analysis cards now label the score simply `Production CMS`, matching the War and Peace presentation instead of exposing evaluator-count language.

No Artifacts page TSX is included.
No corpus values are changed.
