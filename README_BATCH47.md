# TORC Batch 47 — Desktop Ochre Ground + Gold Process Trial

Apply after Batch 46.

Desktop-only color trial:
- Uses the existing light ochre/gold family from the Submit question/focus controls as the page ground (`#ddceb3`).
- Makes the full Diagnose → Find the gap → Intervene → Test again field the stronger selected-gold material (`#d7c29d`).
- Keeps oxblood outline, arrows, headings, and side rails.
- Mobile/tablet are intentionally untouched from Batch 46 so the already-strong mobile appearance can be judged independently.

No copy, pricing behavior, form behavior, routes, navigation, Theory page, or artifact data changes.
