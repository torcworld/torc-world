# TORC Batch 63 — Artifacts Submit-Family Header + Hard Active Nav

Apply after Batch 62.

Navigation:
- Adds a literal `torc-active-nav` class to the current Artifacts / Theory / Submit route.
- This is an independent second active-state mechanism on top of `aria-current` and the CSS-module active class.
- The current page now keeps the stronger gold `#c7aa76`; non-current buttons remain the lighter resting gold.

Artifacts:
- Reworks the top Artifacts area into the same visual family as the Submit Your Artifact arrival panel:
  charcoal field, thin oxblood top line, large white `Artifacts`, subtle horizontal oxblood accent.
- Keeps `Curated TORC analyses`, but restyles it as a restrained warm serif subtitle rather than a small red eyebrow.
- Reworks the search/filter area into the same family as Diagnose / Find the Gap / Intervene / Test Again:
  lighter navigation gold `#d9c7a6`, oxblood structural border and labels, quiet off-white input fields.
- Existing side rails, corpus table, sorting, filters, pagination and Compare are untouched.

No artifact data, analysis content, scores, routes, Submit content, Theory composition, or artifact-analysis framing are changed.
