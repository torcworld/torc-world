TORC Batch 20 — Submit package selection + question focus
Apply after TORC_BATCH19_SUBMIT_CONVERSION_FIXED.zip.

Changes:
- The currently selected package is the warm gold/brown card; the other two return to the neutral surface.
- Selection is restored from ?package= evaluation/development/deep.
- Adds a prepared question dropdown showing the kinds of structural questions TORC can answer.
- Keeps a separate optional free-text field for specific context.
- Re-evaluation/follow-up pricing is deliberately NOT hard-coded yet; that commercial policy still needs a decision.
