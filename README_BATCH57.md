# TORC Batch 57 — Cleaner Artifacts Entrance + Extended Side Architecture

Apply after Batch 56.

Artifacts page only:
- Removes the right-side corpus marker (`55 artifacts / Ranked by demonstrated cognition`).
- Removes the separate `Explore published TORC analyses.` line.
- Keeps the Artifacts title and `Curated TORC analyses` eyebrow as the cleaner entrance.
- Makes the two oxblood side fields thinner.
- Extends those oxblood fields upward and farther down so they read as continuous page architecture rather than short decorative rails.
- Nudges Operational Order heading/value slightly left.
- Nudges Cognitive Magnitude Score heading/value slightly farther left.
- Keeps the numeric values centered within their own result columns after the shift.

No artifact data, filters, sorting, pagination, Compare behavior, scores, routes, or analyses are changed.
