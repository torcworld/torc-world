# TORC Batch 81 — Organizations Application Block Consolidation

Apply after Batch 80.

Changes:
- Reduces the site-ground space below the final oxblood inquiry block by one third.
- Removes the complete `THE PROBLEM / When the volume grows, consistency becomes part of the evaluation.` section.
- Keeps the four-word signal strip beneath the charcoal hero.
- Leaves a short site-ground interval after that strip.
- Rebuilds Applications as one contained ochre field with a thin oxblood top rule.
- Uses `APPLICATIONS / Built for more than one artifact at a time.` as the sole statement at the top of that field.
- Removes the extra `Select a use case...` sentence.
- Makes all four clickable application cards deep TORC gold at rest, rather than only on hover/open.
- Hover/focus deepens the gold slightly.
- Leaves the `Fifty artifacts a week?` and `One corpus. A clearer view.` sections in place for the next decision rather than deleting both at once.

No TORC methodology, institutional claims, artifact data, navigation, or routes are changed.
