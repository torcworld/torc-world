# TORC — Book Author Spacing Refinement V2

Overlay/update patch — not a complete repository.

- Moves only the `by G. K. Muijlaert` line farther down.
- Desktop top gap: 24px (was 18px).
- Mobile top gap: 21px (was 16px).
- Does not move the book title, subtitle, theory-name line, or white book-paper section.
- Keeps the existing author size and spacing between `by` and the name.
