# TORC — Thus Spoke Zarathustra update

Overlay patch for the current source-of-truth repository. Adds the canonical 3c | 825 artifact registry entry, artifact page, consensus report, seven evaluator reports, and six-dimensional profile.

**This ZIP is a PATCH/UPDATE, not a complete website repository.** Copy its contents over the current working repository, preserving all other files.
