# TORC Batch 65 — Submit-like Framing for Artifacts + Analysis

Apply after Batch 64.

Artifacts page:
- Adds a little open space below the global navigation.
- Adds a very quiet translucent charcoal horizontal rule across the page.
- Leaves a further breathing zone before the real Artifacts composition begins.
- Thin oxblood vertical rules then frame the full Artifacts composition, echoing the Submit Your Artifact page architecture.
- The frame contains the charcoal Artifacts hero, the search/filter strip and the unified gold corpus field.
- The charcoal Artifacts hero gets more presence: `Artifacts` is larger and centered; the curated-analysis line becomes a restrained institutional gold label.

Artifact analysis pages:
- Preserves the accepted right-side floating oxblood field and its Batch 61 position.
- Frames the left analysis column with thin oxblood vertical rules.
- Converts the title / creator / Operational Order / CMS hero area to charcoal grey with a thin oxblood top/bottom structural edge.
- Keeps the large title, Order and CMS hierarchy intact; only the material treatment changes.
- Mobile removes the outer vertical frame and keeps the analysis readable.

Navigation is deliberately untouched in this patch.
No artifact data, scores, filters, sorting, pagination, analysis text or routes are changed.
